export class calculationdetails{
    HCalStop_Date: Date;  // Date
    HCalStop_Allyn: string;  // Varchar → string

    HSalStop_yn: string;  // Varchar → string
    HSalStop_Date: Date;  // Date
    HSalStop_Rem_SysID: number;  // Number
    HSalStop_Rem_Code: string;  // Varchar → string
    HSalStop_Rem_Name: string;  // Varchar → string

    HOverStop_yn: string;  // Varchar → string
    HOverStop_Date: Date;  // Date
    HOverStop_Rem_SysID: number;  // Number
    HOverStop_Rem_Code: string;  // Varchar → string
    HOverStop_Rem_Name: string;  // Varchar → string

    HLSalStop_yn: string;  // Varchar → string
    HLSalStop_Date: Date;  // Date
    HLSalStop_Rem_SysID: number;  // Number
    HLSalStop_Rem_Code: string;  // Varchar → string
    HLSalStop_Rem_Name: string;  // Varchar → string

    HLGratStop_yn: string;  // Varchar → string
    HLGratStop_Date: Date;  // Date
    HLGratStop_Rem_SysID: number;  // Number
    HLGratStop_Rem_Code: string;  // Varchar → string
    HLGratStop_Rem_Name: string;  // Varchar → string
}